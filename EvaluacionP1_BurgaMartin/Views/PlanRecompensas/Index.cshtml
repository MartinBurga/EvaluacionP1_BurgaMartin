@model IEnumerable<EvaluacionP1_BurgaMartin.Models.PlanRecompensas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.fechaInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.pntos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cliente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fechaInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.pntos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cliente.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPlan">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPlan">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPlan">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
